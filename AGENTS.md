# 钢铁守卫开发指南

## 目标与约束

这是一个纯前端、原创 8-bit 坦克防线游戏。修改玩法或验收范围前先阅读 `PLAN.md`；面向玩家的使用说明维护在 `README.md`。

- 保持原创像素图形、合成音效和原创关卡，不引入原作 ROM、素材、音乐、音效或关卡布局。
- 首版目标是桌面浏览器、本地单人/双人；在线服务、账号、云存档和触控操作不属于当前范围。
- 中文用于界面文案，代码标识符和序列化字段使用英文。

## 架构边界

- `simulation.ts` 是确定性规则核心。它只能依赖领域类型和关卡数据，不得读取 DOM、Canvas、音频、真实时间或无种子随机数。
- `renderer.ts` 和 `audio.ts` 只消费 `WorldSnapshot` / `SimEvent`，不得直接修改世界状态。
- `input.ts` 将键盘和前两个已连接的 Gamepad 统一为 `PlayerCommand`。
- `main.ts` 负责场景流转、固定步长循环和运行时资源清理；场景退出时必须撤销 RAF、定时器和事件监听器。
- `levels.ts` 的内置关卡与编辑器关卡共用 `LevelSchema` 和 `validateLevel`。不要为内置数据建立绕过校验的路径。
- `persistence.ts` 是存档版本与迁移的唯一入口。存档写入按事务处理：写入失败时保留原内存状态，并向用户显示真实错误。
- `editor.ts` 保存的是版本化关卡 JSON；自制关卡的成绩和试玩结果不得写入战役进度或最高分。

## 必须保持的不变量

- 模拟采用固定 60 Hz tick；相同关卡、种子和输入序列必须得到相同快照与事件。
- AI 随机行为统一使用世界持有的带种子随机数；洗牌使用确定性的 Fisher–Yates，不使用随机比较器排序。
- Canvas 逻辑分辨率固定为 256×240，关闭图像平滑，仅以 1×、2×或 3×整数倍显示并保持宽高比。
- 每个战役关卡包含恰好 20 个敌军，同屏活动敌军不超过 4 个；35 个内置关卡必须全部通过统一校验。
- 关卡校验覆盖尺寸、枚举值、唯一基地、出生点占用与重叠、敌军队列、数值边界、连通活动空间和基地可防守性。
- 页面失焦清空输入并暂停；恢复时丢弃积压时间，不补跑旧 tick。
- 暂停、开场和终局阶段不推进游戏规则；终局后禁用会产生竞态的控制。
- 渲染顺序保持地形 → 道具 → 实体/子弹 → 树林前景 → HUD/覆盖层。
- 定时道具使用 tick 计时，并为警告和到期发出领域事件；基地强化墙在结束前闪烁并恢复原地形。

## 修改流程

1. 先定位变更属于模拟、输入、表现、场景、关卡、存档还是编辑器边界。
2. 玩法修复优先在 `GameWorld` 的公开创建/命令/step/快照事件接口上复现；保持渲染和音频为只读消费者。
3. 修改 `LevelSchema`、`SaveData` 或公开联合类型时，同步更新校验、迁移、编辑器、测试和文档。
4. 新增关卡或改变生成逻辑时，运行全部 35 关 release gate，不只抽查单关。
5. UI 或浏览器生命周期改动同时验证生产构建和对应 Playwright 流程。
6. 完成前检查格式、静态分析、单测、构建和受影响的跨浏览器流程。

## 验证命令

```bash
npm run format:check
npm run lint
npm test
npm run build
npm run test:e2e
```

- 规则、关卡、存档和输入行为使用 Vitest。
- 菜单、持久化、焦点/暂停、编辑器、Canvas 比例及完整胜负流程使用 Playwright。
- `npm run test:e2e` 会先构建并启动 Vite preview，并在 Chromium、Edge、Firefox 上运行。
- 只测试可观察行为；必要的测试夹具应服务于场景构造，断言以快照、事件或用户可见结果为准。

## 完成标准

改动直接满足任务要求，未破坏上述不变量；相关回归测试已添加；格式、lint、类型检查、构建和适用测试均通过；没有提交生成缓存、测试产物、调试代码或受版权保护的素材。
